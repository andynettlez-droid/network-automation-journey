---
# Day 1 lab — YOU write the config task.
#
# Declare an IPv4 address on ethernet-1/1 so that running this playbook a SECOND
# time makes NO change. Use a state-declaring module (nokia.srlinux.config with an
# `update:` block), not a raw command push.
#
# Run #1 should report "changed"; run #2 should report "ok" / no change.
# Stuck? The answer is in solution/ — but attempt it first; the struggle is the lesson.
#
#   ansible-playbook -i inventory.yml configure-interface.yml   # run 1
#   ansible-playbook -i inventory.yml configure-interface.yml   # run 2
- name: "Day 1 — idempotent interface config on srl1"
  hosts: srl1
  gather_facts: false
  tasks:
    - name: "TODO: declare ethernet-1/1 IPv4 state here"
      ansible.builtin.debug:
        msg: "Replace me with an idempotent nokia.srlinux.config task."
    # Verify on the device, not this recap:
    #   docker exec -it clab-day1-idempotency-srl1 sr_cli "show interface ethernet-1/1"
